const fs=require('fs'),zlib=require('zlib');
let source=zlib.gunzipSync(Buffer.from(fs.readFileSync('server.bundle.txt','utf8').trim(),'base64')).toString('utf8');
source=source.replace("function resumeLimitFor(session){return resumeTier(session)==='free'?3:8;}\n","function resumeLimitFor(session){return resumeTier(session)==='free'?3:8;}\nfunction resumeRiskLimitFor(session){return resumeTier(session)==='free'?2:5;}\n");
source=source.replace("function buildResumePlan(facts,skills,limit){\n","function buildResumeRiskMap(facts,skills,limit){\n  const f=(facts||[]).map(sanitizeResumeFact).filter(Boolean).slice(0,10);\n  const sk=(skills||[]).map(sanitizeResumeFact).filter(Boolean).slice(0,8);\n  const metricRe=/\\b\\d+(?:\\.\\d+)?%|[$£€¥]\\s?\\d|\\b\\d{2,}(?:[,+]\\d+)*\\b/i;\n  const leadershipRe=/\\b(led|managed|oversaw|supervised|owned|directed|coordinated|headed|responsible for)\\b/i;\n  const impactRe=/\\b(improved|reduced|increased|saved|streamlined|optimized|delivered|achieved|grew|cut|prevented|resolved)\\b/i;\n  const judgmentRe=/\\b(audit|risk|compliance|quality|control|investigated|validated|verified|reconciled|forecast|budget|variance|accuracy|exception|anomal|discrep|decision|judgment)\\w*\\b/i;\n  const stakeholderRe=/\\b(team|client|stakeholder|management|leadership|cross-functional|department|vendor|customer|staff)\\b/i;\n  const vagueRe=/\\b(significant|substantial|major|various|multiple|successful|excellent|extensive|responsible for|assisted with|helped)\\b/i;\n  const scored=f.map((anchor,index)=>{\n    let score=1;const signals=[];const defend=[];const follow=[];\n    const hasMetric=metricRe.test(anchor), hasLeadership=leadershipRe.test(anchor), hasImpact=impactRe.test(anchor), hasJudgment=judgmentRe.test(anchor), hasStakeholder=stakeholderRe.test(anchor), hasVague=vagueRe.test(anchor);\n    const matchingSkills=sk.filter(skill=>new RegExp('\\\\b'+skill.replace(/[.*+?^${}()|[\\\\]\\\\]/g,'\\\\$&').replace(/\\\\ /g,'\\\\s+')+'\\\\b','i').test(anchor)).slice(0,2);\n    if(hasMetric){score+=5;signals.push('Quantified claim');defend.push('Number: know exactly what the figure measures, the period covered, the source, and how it was verified.');follow.push('How was that number calculated, and what evidence would let someone verify it?');}\n    if(hasLeadership){score+=4;signals.push('Ownership / leadership');defend.push('Scope: separate what you personally owned from what the team, manager, or client owned.');follow.push('What decisions were yours, and what work belonged to other people?');}\n    if(hasImpact){score+=3;signals.push('Outcome / impact');defend.push('Attribution: be ready to explain what changed, your contribution, and what else may have influenced the result.');follow.push('What specifically did you do that contributed to that outcome?');}\n    if(hasJudgment){score+=3;signals.push('Judgment / quality risk');defend.push('Decision: know the standard, evidence, validation step, or control you used when judgment mattered.');follow.push('What standard or evidence did you use when the answer was not obvious?');}\n    if(hasStakeholder){score+=2;signals.push('Stakeholder complexity');defend.push('Communication: identify who needed what, where disagreement or ambiguity existed, and how you confirmed understanding.');follow.push('Who disagreed or needed convincing, and how did you handle it?');}\n    if(matchingSkills.length){score+=2;signals.push('Technical depth');defend.push(`Method: be able to explain how you actually used ${matchingSkills.join(' / ')}, not just name the tool or skill.`);follow.push(`Walk me through how you used ${matchingSkills[0]} step by step in this example.`);}\n    if(hasVague){score+=2;signals.push('Broad wording');defend.push('Specificity: replace broad wording with one concrete example, boundary, result, or decision you can defend.');follow.push('Can you make that claim more specific with one concrete example?');}\n    if(anchor.length>180){score+=1;signals.push('Dense claim');defend.push('Compression: practice explaining the claim in one clear sentence before adding detail.');}\n    defend.unshift('Facts: know the situation, your exact responsibility, the sequence of actions, and what evidence supports the statement.');\n    defend.push('Tradeoff: identify one constraint, risk, alternative, or limitation you had to manage.');\n    defend.push('Lesson: know what you learned, what you would repeat, and what you would change next time.');\n    if(!follow.length) follow.push('What exactly did you personally do, and how did you know your work was correct?');\n    follow.push('What was the hardest part of this example, and what would you do differently now?');\n    const why=signals.length?`This line carries ${signals.slice(0,3).join(', ').toLowerCase()}, which gives an interviewer several natural directions for evidence-based follow-up.`:'This is a substantial resume claim that can invite follow-up on scope, method, evidence, and lessons learned.';\n    return {anchor,score,index,signals:signals.slice(0,4),why,defend:Array.from(new Set(defend)).slice(0,6),followUps:Array.from(new Set(follow)).slice(0,3)};\n  });\n  scored.sort((a,b)=>b.score-a.score||a.index-b.index);\n  return scored.slice(0,Math.max(1,Math.min(limit||2,5))).map((item,i)=>({priority:i+1,...item}));\n}\n\nfunction buildResumePlan(facts,skills,limit){\n");
source=source.replace("accessGateVersion:'v7-resume-customization'","accessGateVersion:'v8-resume-risk-map'");
source=source.replace("const skills=extractResumeSkills(text),tier=resumeTier(session),limit=resumeLimitFor(session),questions=buildResumePlan(facts,skills,limit);\n      return send(res,200,{ok:true,tier,facts,skills,questions,upsell:tier==='free',privacy:'processed-in-memory-not-stored'});","const skills=extractResumeSkills(text),tier=resumeTier(session),limit=resumeLimitFor(session),questions=buildResumePlan(facts,skills,limit),riskMap=buildResumeRiskMap(facts,skills,resumeRiskLimitFor(session));\n      return send(res,200,{ok:true,tier,facts,skills,riskMap,questions,upsell:tier==='free',privacy:'processed-in-memory-not-stored'});");
source=source.replace("const joined=facts.join(' '),skills=extractResumeSkills(joined),tier=resumeTier(session),questions=buildResumePlan(facts,skills,resumeLimitFor(session));\n      return send(res,200,{ok:true,tier,facts,skills,questions,upsell:tier==='free',privacy:'facts-submitted-for-plan-not-stored'});","const joined=facts.join(' '),skills=extractResumeSkills(joined),tier=resumeTier(session),questions=buildResumePlan(facts,skills,resumeLimitFor(session)),riskMap=buildResumeRiskMap(facts,skills,resumeRiskLimitFor(session));\n      return send(res,200,{ok:true,tier,facts,skills,riskMap,questions,upsell:tier==='free',privacy:'facts-submitted-for-plan-not-stored'});");
source=source.replace("<div class=\"item\"><b>Full Resume Custom Plan</b><span>Eight personalized Zara-style questions built only from resume evidence you confirm.</span></div>","<div class=\"item\"><b>Resume Risk Map + Custom Plan</b><span>Up to five follow-up pressure points plus eight personalized questions built only from résumé evidence you confirm.</span></div>");
if(!source.includes("accessGateVersion:'v8-resume-risk-map'")||!source.includes('buildResumeRiskMap'))throw new Error('V8 server patch failed');
new Function('require','__dirname','__filename',source)(require,__dirname,__filename);
